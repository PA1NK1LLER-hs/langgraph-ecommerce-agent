# 今日工作总结报告（2026-08-13）

会话主题：双模型路由优化、运行时模型回退、知识库更新链路修复、账号角色修复。
所有改动均已实现并通过端到端验证（仓库仍为零提交状态，改动在工作区未提交）。

---

## 一、双模型路由策略调整 + 运行时回退（约 90% 请求走 Flash）

**需求**：agent 没有长时间编码需求，Flash 已足够，希望约 90% 回答走 Flash；同时补上运行时回退（原三级回退只覆盖实例创建失败，API 调用失败无兜底）。

### 1. 路由规则（`src/agent/graph.py`）

旧规则只要 `need_rpa or intent=="complex" or len(input)>200 or len(messages)>10` 就上 Pro，
导致大多数稍长/多轮对话都走 Pro。新规则**仅两类场景走 Pro**：

| 场景 | 条件 |
|---|---|
| RPA 任务（高风险自动化） | `need_rpa == True` |
| 高难度复杂请求 | `intent == "complex"` 且（输入 > 1000 字 或 会话 > 30 条消息） |

其余全部走 Flash。阈值可经环境变量调整（`src/config.py`）：

- `PRO_ROUTE_MIN_CHARS`（默认 1000）
- `PRO_ROUTE_MIN_MESSAGES`（默认 30）

路由决策新增 INFO 日志行（`路由决策: need_rpa=... intent=... user_len=... msg_count=...`），排查路由问题直接 grep backend.log。

### 2. 运行时回退链（`src/agent/graph.py` call_model）

单一 `ainvoke` 改为按序尝试，任一级调用失败自动切换下一级：

```
路由选定模型 → 另一档模型（flash ↔ pro）→ LLM_FALLBACK_MODEL → LLM_FALLBACK_MODEL_2
```

- 每级失败记 WARNING 日志；全部失败抛 `RuntimeError`（携带最后错误）。
- 回退成功后：`_model_used` 记录实际模型（前端模型标签如实显示）、`route_reason` 追加 `(fallback: xxx)`、成本统计按实际模型计算、语义缓存写入实际使用的模型 key。
- 工具绑定从「路由时一次性 bind」改为「每次尝试按 tools_to_bind 单独 bind」（移除 `active_llm` 变量）。
- `build_agent` 预创建 `LLM_FALLBACK_MODEL` / `LLM_FALLBACK_MODEL_2` 实例（去重后加入回退链，创建失败仅 WARNING 不阻断）。

### 3. 验证（新增 `scripts/verify_model_routing.py`）

| 用例 | 结果 |
|---|---|
| 13 字短查询 | `deepseek-v4-flash` ✓ |
| 1196 字 complex 查询 | 6 次 agent 迭代全部 `deepseek-v4-pro`（日志确认）✓ |
| `LLM_FLASH_MODEL` 设为无效值启动后端，全新短查询 | 日志 `模型 deepseek-v4-flash-invalid 调用失败（回退链 1/2）` + `模型回退生效: ... → deepseek-v4-pro`，回复正常 ✓ |

排查备注：短查询曾「误报 flash」——同文案此前已入语义缓存（精确 hash 命中），缓存回复直接沿用存储的 model_key，并非路由失效；判定路由需用全新文案或看 `路由决策` 日志。

---

## 二、知识库更新链路修复（删旧→导新）

**需求**：知识库对应关系变化后如何更新。分析发现三个缺陷：
① 重复上传同一 source 只增量合并，旧实体/关系不会被覆盖；
② DELETE 端点坏（import 不存在的函数必 500、且把 source 名当 doc_id 传）；
③ 前端没有更新入口。

### 修复内容

| 文件 | 修改 |
|---|---|
| `src/rag/indexer.py` | 新增公开方法 `delete_source_async(source)`：按 `file_path == source` 遍历 doc_status 逐个调用 LightRAG `adelete_by_doc_id`（级联删除分块与仅属于该文档的实体/关系） |
| `src/api/routers/knowledge.py` | ① `DELETE /api/kb/sources/{id}` 重写为调用 `delete_source_async`；② 新增 `POST /api/kb/reindex`（multipart：新文件 + source 表单字段），流程 = 先删旧 → 解析 → 逐块重新索引（带进度）；③ 清理死代码 `_validate_reindex_path` / `ReindexRequest` |
| `frontend/src/api/client.ts` | `reindexSource(source, file)` 改为 multipart POST `/api/kb/reindex` |
| `frontend/src/views/KBManagement.vue` | 来源行新增「更新」按钮（两步确认，3 秒超时），隐藏文件输入，更新状态/错误展示 |
| `frontend/dist/` | 已重新构建 |

### 验证（`scripts/verify_kb_update.py`，Neo4j 直查关系边，全部 PASS）

| 步骤 | 结果 |
|---|---|
| upload v1（供应商A→产品B） | 图中 A→B 边存在 ✓ |
| reindex v2（供应商A→产品C） | `removed_old=2`、A→C 边存在、**A→B 边消失** ✓ |
| DELETE source | 实体从图与 stats 中消失 ✓ |

验证方法备注：不用 `/api/kb/search` 断言——dense 搜索在 Qdrant 无命中时走 Neo4j 全文兜底，`search_labels` 对中文查询按字面匹配返回无关实体，且 `_search_structured_async` 的 chunk 正则会把「## 匹配实体」段的实体 ID 误解析为结果内容。关系验证用 cypher-shell 直查图（实体节点 `entity_id` 属性、边 `DIRECTED`）。

---

## 三、账号角色修复：admin 提为 editor

发现 KB 管理接口（upload/reindex/delete）要求 **editor+** 角色，而 users 表全部 4 个账号原先都是 viewer（含名为 admin 的账号）——即 KB 管理页对所有人 403。

- **已修复**：`UPDATE users SET role='editor' WHERE username='admin'`（即时生效，`require_editor` 每次请求从数据库现查角色，无需重新登录）。
- **遗留**：其余 3 个账号（alice、demo、claude_verify）仍是 viewer；注册流程默认 viewer 且无角色提升机制，新注册账号依然无法管理 KB。彻底修复需改注册逻辑（如首个账号自动 editor 或增加管理员提升入口），尚未实施。

---

## 四、文件清单

**修改**：`src/agent/graph.py`、`src/config.py`、`.env.example`、`src/rag/indexer.py`、`src/api/routers/knowledge.py`、`frontend/src/api/client.ts`、`frontend/src/views/KBManagement.vue`、`frontend/dist/`（重建）、`ISSUES_AND_FIXES.md`（新增第十二、十三节）。

**新增**：`scripts/verify_model_routing.py`、`scripts/verify_kb_update.py`。

**数据库变更**：`users.role`（admin：viewer → editor）。

---

## 五、遗留问题与建议

1. **KB 管理权限**：注册默认 viewer + 无提升机制（见第三节遗留）。
2. **dense 搜索中文兜底质量**（既有问题，非本次引入）：Qdrant 无命中时 Neo4j 全文搜索对中文查询按字面匹配，返回无关实体；搜索结果解析正则误匹配实体段。建议后续改进兜底搜索（如用嵌入/关键词检索代替字面 label 匹配）。
3. **一次性未定论异常**：1196 字 complex 查询曾有一次显示 flash（不可复现），加路由日志后行为始终正确；疑似语义缓存干扰。已记录于 ISSUES_AND_FIXES.md 第十二节。

---

## 六、环境状态说明（本次会话结束时）

- 后端服务（`.venv/Scripts/python.exe run.py`）**已停止**（PID 10992 已终止，`/api/health` 不可达）。
- Docker 容器（neo4j / qdrant / postgres）**仍在运行**，如需停止：`docker compose stop`（或 `docker compose down`）。
- 冒烟账号 `claude_verify` 角色已还原为 viewer（测试期间临时提为 editor）。
- 本次改动**尚未提交**（仓库历史为零提交），如需入库请告知。

---

## 附：相关文档索引

- `ISSUES_AND_FIXES.md` 第十二节 — 双模型路由调整 + 运行时回退
- `ISSUES_AND_FIXES.md` 第十三节 — 知识库更新链路修复
- `scripts/verify_model_routing.py` / `scripts/verify_kb_update.py` — 可重复执行的回归验证脚本
